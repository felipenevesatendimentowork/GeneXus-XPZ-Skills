interface:
  display_name: "XPZ KB Parallel Pre-Push"
  short_description: "Validate a KB parallel-folder state before push."
  default_prompt: "Use $xpz-kb-parallel-pre-push to validate the GeneXus KB parallel-folder state before pushing that KB (not the skills repo pre-push)."

policy:
  allow_implicit_invocation: true
